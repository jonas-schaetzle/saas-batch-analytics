version: 2

sources:
  - name: raw
    description: Raw synthetic SaaS source tables loaded into DuckDB for analytics engineering workflows.
    schema: main
    loaded_at_field: loaded_at
    freshness:
      warn_after: {count: 36, period: hour}
      error_after: {count: 72, period: hour}
    tables:
      - name: raw_accounts
        description: Raw account records for SaaS customers.
        columns:
          - name: account_id
            tests:
              - not_null
              - unique
          - name: loaded_at
            tests:
              - not_null

      - name: raw_subscriptions
        description: Raw subscription records associated with customer accounts.
        columns:
          - name: subscription_id
            tests:
              - not_null
              - unique
          - name: account_id
            tests:
              - not_null
          - name: loaded_at
            tests:
              - not_null

      - name: raw_feature_usage
        description: Raw product feature usage events linked to subscriptions.
        columns:
          - name: usage_id
            tests:
              - not_null
          - name: subscription_id
            tests:
              - not_null
          - name: loaded_at
            tests:
              - not_null

      - name: raw_support_tickets
        description: Raw support ticket records linked to customer accounts.
        columns:
          - name: ticket_id
            tests:
              - not_null
              - unique
          - name: account_id
            tests:
              - not_null
          - name: loaded_at
            tests:
              - not_null

      - name: raw_churn_events
        description: Raw churn event records linked to customer accounts.
        columns:
          - name: churn_event_id
            tests:
              - not_null
              - unique
          - name: account_id
            tests:
              - not_null
          - name: loaded_at
            tests:
              - not_null
