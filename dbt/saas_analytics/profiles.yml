saas_analytics:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: saas_analytics.duckdb
      schema: main
    prod:
      type: duckdb
      path: saas_analytics.duckdb
      schema: prod
